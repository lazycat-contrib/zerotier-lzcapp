manifest: ./lzc-manifest.yml
pkgout: ./dist
icon: ./icon.png
contentdir: ./content

compose_override:
  services:
    zerotier:
      devices:
        - /dev/net/tun
      cap_add:
        - NET_ADMIN
        - SYS_ADMIN
        - NET_RAW
